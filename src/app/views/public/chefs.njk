{% extends "public/layout.njk" %}

{% block content %}
<section class="chefs">
    {% for chef in chefs %}
        <div class="card_chef">
            <img src="{{chef.avatar_url}}" alt="Imagem do chef {{chef.name}}">
            <div>
                <h4>{{chef.name}}</h4>
                <p>{% if chef.recipes_chef == 0 or chef.recipes_chef == 1 %}{{chef.recipes_chef}} Receita {% else %} {{chef.recipes_chef}} Receitas{% endif %}</p>
            </div>
        </div>
    {% endfor %}
</section>
{% endblock content %}
<p>PÁGINA DOS CHEFS</p> 